openapi: 3.0.3
info: { title: Fixture, version: 0.0.0 }
paths:
  /internal/v1/search:
    post:
      operationId: fixtureSearch
      responses:
        "200":
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Composed" }
components:
  schemas:
    Composed:
      oneOf:
        - type: object
        - type: string
