=== D2b-auth-prefix-only-comparison ===
repo:      yegamble/vizra-search
baseline:  7abf36918beaad9116deeb24f6696446592bd7ba (feat/m0-minimal-service)
date:      2026-09-20T17:59:06Z
toolchain: go version go1.27.1 darwin/arm64
host:      Darwin arm64

D2: the MAC comparison covers the whole value.
Mutation: compare only the first byte of the MAC. This mutation originally
SURVIVED the suite, because two unrelated MACs almost always differ in the
first byte; the three tests added in commit 7abf369 close that gap.

--- mutation applied ---
diff --git a/internal/hmacauth/hmacauth.go b/internal/hmacauth/hmacauth.go
index de6c901..0059e75 100644
--- a/internal/hmacauth/hmacauth.go
+++ b/internal/hmacauth/hmacauth.go
@@ -229,3 +229,3 @@ func (v *Verifier) Verify(method, path string, header http.Header, body []byte)
 	// much of the MAC the caller guessed correctly.
-	if !hmac.Equal(provided, expected) {
+	if len(provided) > 0 && provided[0] != expected[0] { // MUTATION: compare one byte only
 		return reject(ReasonBadSignature)

--- RED: go test -count=1 ./internal/hmacauth/ ./internal/httpapi/ ---
--- FAIL: TestVerifyRejectsASignatureThatDiffersOnlyInTheLastByte (0.00s)
    hmacauth_test.go:355: expected rejection with reason "bad_signature", got nil
--- FAIL: TestVerifyUsesTheDocumentedConstantTimePrimitive (0.00s)
    hmacauth_test.go:387: the MAC comparison no longer goes through hmac.Equal; a non-constant-time comparison leaks how much of a forgery was correct
FAIL
FAIL	github.com/yegamble/vizra-search/internal/hmacauth	0.341s
ok  	github.com/yegamble/vizra-search/internal/httpapi	0.629s
FAIL
[exit code: 1]

--- restore (git checkout -- .) ---
working tree is clean at 7abf36918beaad9116deeb24f6696446592bd7ba

--- GREEN: go test -count=1 ./internal/hmacauth/ ./internal/httpapi/ ---
ok  	github.com/yegamble/vizra-search/internal/hmacauth	0.218s
ok  	github.com/yegamble/vizra-search/internal/httpapi	0.386s
[exit code: 0]
