# Fix round 1, BEFORE: this round's committed scripts/scripts_test.go run against 617c6d9's UNMODIFIED
# scripts/makegate.py and scripts/make-integrity-guard.py (detached worktree at 617c6d9; only the test file copied in; nothing committed there).
# Expected: every new pre-make refusal row, both new reading rows, and both M-4 callers FAIL; the rows that existed at 617c6d9 still pass.
# cwd: the 617c6d9 worktree. go version go1.27.1 darwin/arm64; GNU Make 3.81; python 3.9.6; 2026-09-23T12:57:24Z
# sha256 of the gate files under test: 0f66edb377aa0491b2fe0b0829da5495576dd947e3cace36a99e64e4f9d8b20e  scripts/makegate.py 1118e6adb1edf8b92f813176cc53771fcdb7ade35ab6cbd8cfc5a14474aa9b9b  scripts/make-integrity-guard.py 
$ go test -count=1 -v -run 'TestNamedMakefileConstructsAreRefusedBeforeMake|TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes|TestEnvironmentTakenVariablesNeverReachMake' ./scripts/
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_the_test_recipe
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_the_test_recipe
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_on_the_test_recipe
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_on_the_test_recipe
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/;_true_on_the_test_recipe
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/;_true_on_the_test_recipe
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/the_exempt_line_in_another_target
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/the_exempt_line_in_another_target
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/duplicate_test_target
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/duplicate_test_target
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/test_target_inside_a_conditional
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/test_target_inside_a_conditional
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_produced_by_an_expansion
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_produced_by_an_expansion
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/a_NEW_?=_variable,_set_in_the_environment
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/a_NEW_?=_variable,_set_in_the_environment
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_inside_a_conditional_within_the_test_recipe
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_inside_a_conditional_within_the_test_recipe
=== RUN   TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_a_lane_reached_from_a_ci_line_whose_comment_holds_=
=== PAUSE TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_a_lane_reached_from_a_ci_line_whose_comment_holds_=
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_the_test_recipe
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/test_target_inside_a_conditional
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/the_exempt_line_in_another_target
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_on_the_test_recipe
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/duplicate_test_target
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_inside_a_conditional_within_the_test_recipe
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/a_NEW_?=_variable,_set_in_the_environment
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/;_true_on_the_test_recipe
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_inside_a_conditional_within_the_test_recipe
    scripts_test.go:659: - prefix inside a conditional within the test recipe: exit 0, want a refusal naming "prefixed `-`":
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes-_p187600745/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 43313aa38b53… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:158)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:199)
          ok    gate target `test-noskip` is defined exactly once (Makefile:151)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_produced_by_an_expansion
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/a_NEW_?=_variable,_set_in_the_environment
    scripts_test.go:671: a NEW ?= variable, set in the environment | Makefile sha256 e9d7c58edc93 -> f663b1133eb0 (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  the environment sets GOCMD='true', and this is the WORKFLOW anchor. | restored: GREEN
=== CONT  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_a_lane_reached_from_a_ci_line_whose_comment_holds_=
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_the_test_recipe
    scripts_test.go:671: - prefix on the test recipe | Makefile sha256 e9d7c58edc93 -> 18109e835a5d (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  Makefile:138 — gate target `test` has a recipe line prefixed `-`: `-go test -race -count=1 $(PKG)` | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/duplicate_test_target
    scripts_test.go:671: duplicate test target | Makefile sha256 e9d7c58edc93 -> ae6f5a64f3ed (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  gate target `test` is defined 2 times: Makefile:137, Makefile:231 | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_on_the_test_recipe
    scripts_test.go:671: || true on the test recipe | Makefile sha256 e9d7c58edc93 -> 75833c438453 (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  Makefile:138 — gate target `test` has a recipe line ending `|| true`: `go test -race -count=1 $(PKG) || true` | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/the_exempt_line_in_another_target
    scripts_test.go:671: the exempt line in another target | Makefile sha256 e9d7c58edc93 -> 755b8d775709 (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  Makefile:64 — gate target `vet` has a recipe line ending `|| true`: `go test -count=1 -json $(DRIFT_PKGS) > $(DRIFT_REPORT) || true` | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/test_target_inside_a_conditional
    scripts_test.go:671: test target inside a conditional | Makefile sha256 e9d7c58edc93 -> bbd1bb62a86d (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  Makefile:138 defines the gate target `test` inside a make conditional. | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/;_true_on_the_test_recipe
    scripts_test.go:671: ; true on the test recipe | Makefile sha256 e9d7c58edc93 -> ac972d3ceb6f (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  Makefile:138 — gate target `test` has a recipe line ending `; true`: `go test -race -count=1 $(PKG); true` | restored: GREEN
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_a_lane_reached_from_a_ci_line_whose_comment_holds_=
    scripts_test.go:659: - prefix on a lane reached from a ci line whose comment holds =: exit 0, want a refusal naming "gate target `inert-lane` has a recipe line prefixed `-`":
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes-_p631502735/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 d97e5aad0637… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 9 target(s); 0 reached through prerequisites
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:57)
          ok    gate target `vet` is defined exactly once (Makefile:65)
          ok    gate target `echo-containment` is defined exactly once (Makefile:69)
          ok    gate target `build` is defined exactly once (Makefile:78)
          ok    gate target `contract-drift` is defined exactly once (Makefile:134)
          ok    gate target `test` is defined exactly once (Makefile:140)
          ok    gate target `tidy-check` is defined exactly once (Makefile:158)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:199)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== NAME  TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_produced_by_an_expansion
    scripts_test.go:671: || true produced by an expansion | Makefile sha256 e9d7c58edc93 -> a24953d099c2 (re-pinned, as a reviewer would) | digest ok, RED exit 1: FAIL  `ci` EXPANDS to a command whose exit status is discarded: `go test -race -count=1 ./... || true` | restored: GREEN
--- FAIL: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes (0.05s)
    --- FAIL: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_inside_a_conditional_within_the_test_recipe (1.88s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/a_NEW_?=_variable,_set_in_the_environment (2.05s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_the_test_recipe (3.61s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/duplicate_test_target (3.62s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/the_exempt_line_in_another_target (3.64s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_on_the_test_recipe (3.64s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/test_target_inside_a_conditional (3.66s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/;_true_on_the_test_recipe (3.72s)
    --- FAIL: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/-_prefix_on_a_lane_reached_from_a_ci_line_whose_comment_holds_= (1.73s)
    --- PASS: TestMakeIntegrityGuardStillRefusesKnownShapesInReviewedBytes/||_true_produced_by_an_expansion (2.71s)
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.RECIPEPREFIX_:=_>_with_a_>_-true_recipe
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.RECIPEPREFIX_:=_>_with_a_>_-true_recipe
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/override_.RECIPEPREFIX_=_>
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/override_.RECIPEPREFIX_=_>
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/define_.RECIPEPREFIX
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/define_.RECIPEPREFIX
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.SECONDEXPANSION:
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.SECONDEXPANSION:
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.ONESHELL:
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.ONESHELL:
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_(all_targets)
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_(all_targets)
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_test
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_test
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.DEFAULT:
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.DEFAULT:
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.POSIX:
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.POSIX:
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.EXTRA_PREREQS
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.EXTRA_PREREQS
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_.EXTRA_PREREQS
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_.EXTRA_PREREQS
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/SHELL_:=_/usr/bin/true
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/SHELL_:=_/usr/bin/true
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/.SHELLFLAGS_:=_-c
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/.SHELLFLAGS_:=_-c
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/MAKEFLAGS_+=_-i
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/MAKEFLAGS_+=_-i
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/GNUMAKEFLAGS_+=_-i
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/GNUMAKEFLAGS_+=_-i
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/MFLAGS_=_-i
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/MFLAGS_=_-i
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_MAKEFLAGS
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_MAKEFLAGS
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/pattern-specific_SHELL
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/pattern-specific_SHELL
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_private_SHELL
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_private_SHELL
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/override_.SHELLFLAGS
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/override_.SHELLFLAGS
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/define_MAKEFLAGS
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/define_MAKEFLAGS
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/$(eval_…)
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/$(eval_…)
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/+_recipe_line
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/+_recipe_line
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/$(MAKE)_in_a_recipe
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/$(MAKE)_in_a_recipe
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_an_expansion
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_an_expansion
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$@
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$@
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$<
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$<
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$X_(a_one-letter_variable)
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$X_(a_one-letter_variable)
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_-_prefix
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_-_prefix
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_beginning_with_$@
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_beginning_with_$@
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_+_prefix
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_+_prefix
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/two_targets_on_one_rule_line,_a_gate_target_second
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/two_targets_on_one_rule_line,_a_gate_target_second
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/grouped_targets_&:
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/grouped_targets_&:
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_special_target_that_is_not_the_first_word
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_special_target_that_is_not_the_first_word
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_target_that_is_an_expansion_($(I)ORE:)
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_target_that_is_an_expansion_($(I)ORE:)
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_that_starts_with_whitespace
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_that_starts_with_whitespace
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_continued_with_a_backslash
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_continued_with_a_backslash
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_global
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_global
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_target-specific
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_target-specific
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/define_with_a_computed_name
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/define_with_a_computed_name
=== RUN   TestNamedMakefileConstructsAreRefusedBeforeMake/$(call_eval,…)
=== PAUSE TestNamedMakefileConstructsAreRefusedBeforeMake/$(call_eval,…)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.RECIPEPREFIX_:=_>_with_a_>_-true_recipe
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/$(eval_…)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/two_targets_on_one_rule_line,_a_gate_target_second
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/$(call_eval,…)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/define_with_a_computed_name
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_target-specific
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_global
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$<
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.RECIPEPREFIX_:=_>_with_a_>_-true_recipe
    scripts_test.go:1788: .RECIPEPREFIX := > with a > -true recipe (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.RECIPEPREFIX` (`.RECIPEPREFIX := >`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_continued_with_a_backslash
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$<
    scripts_test.go:1788: recipe that begins with $< (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:232 is a recipe line that BEGINS with an expansion (`$<true`). What it expands to — possibly a `-` or `+` prefix — cannot be determined without running make. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_that_starts_with_whitespace
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/$(eval_…)
    scripts_test.go:1788: $(eval …) (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 calls $(eval …) or $(guile …), which can manufacture an include the static reading cannot see: `$(eval INERT := 1)` | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_target_that_is_an_expansion_($(I)ORE:)
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/$(call_eval,…)
    scripts_test.go:1782: exit 0; want "calls $(eval" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMake$(call_eval,)2513925496/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 9560f72ef9e2… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_+_prefix
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_target-specific
    scripts_test.go:1782: exit 1; want "assigns a variable whose name is an expansion" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_computed_varia2728266661/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 8 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'S', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 8d37fb500725… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          FAIL  gate target `test` is defined 2 times: Makefile:137, Makefile:231
                make runs the LAST definition and REPLACES the earlier recipe. The lane must have one recipe.
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: FAILED
        A make-driven gate that can be turned into a no-op is not a gate. Fix the Makefile.
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/a_special_target_that_is_not_the_first_word
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/two_targets_on_one_rule_line,_a_gate_target_second
    scripts_test.go:1782: exit 1; want "more than one target" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMaketwo_targets_on_o1627519148/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 bf0c68dd8370… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `ci`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `fmt-check`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `vet`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `echo-containment`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `build`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `contract-drift`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `test`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `tidy-check`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          FAIL  make reports a DUPLICATE definition while resolving `vendor-contract-selftest`:
                Makefile:232: warning: overriding commands for target `test'
                Makefile:138: warning: ignoring old commands for target `test'
                make runs the LAST definition. A reader — and any text-based check — sees the first, so a
                duplicate can replace a whole gate recipe with `@true` and leave the file looking correct.
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: FAILED
        A make-driven gate that can be turned into a no-op is not a gate. Fix the Makefile.
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/grouped_targets_&:
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/define_with_a_computed_name
    scripts_test.go:1782: exit 0; want "defines a variable whose name is an expansion" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakedefine_with_a_co3439575628/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 8 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'M', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 601a9ab68269… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_beginning_with_$@
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_global
    scripts_test.go:1782: exit 0; want "assigns a variable whose name is an expansion" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_computed_varia1901617922/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 8 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'M', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 2c585f79c5aa… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$X_(a_one-letter_variable)
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_continued_with_a_backslash
    scripts_test.go:1782: exit 0; want "continued with a backslash" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_rule_line_cont2210075480/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 769ae7c41a4c… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/SHELL_:=_/usr/bin/true
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_that_starts_with_whitespace
    scripts_test.go:1782: exit 0; want "starts with whitespace" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_rule_line_that152214114/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 8d65d3010c65… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_test
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_target_that_is_an_expansion_($(I)ORE:)
    scripts_test.go:1782: exit 0; want "target name is an expansion" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_rule_target_th1715588545/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 8 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'I', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 4340451b44c5… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_.EXTRA_PREREQS
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$X_(a_one-letter_variable)
    scripts_test.go:1788: recipe that begins with $X (a one-letter variable) (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:233 is a recipe line that BEGINS with an expansion (`$Xtrue`). What it expands to — possibly a `-` or `+` prefix — cannot be determined without running make. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.EXTRA_PREREQS
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/SHELL_:=_/usr/bin/true
    scripts_test.go:1788: SHELL := /usr/bin/true (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `SHELL` (`SHELL := /usr/bin/true`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.POSIX:
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_.EXTRA_PREREQS
    scripts_test.go:1788: target-specific .EXTRA_PREREQS (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.EXTRA_PREREQS` (`test: .EXTRA_PREREQS := nothing`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/define_MAKEFLAGS
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_test
    scripts_test.go:1788: .IGNORE: test (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.IGNORE`: every recipe failure of the named targets (or of ALL targets) is ignored — a `-` prefix no reading can see. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.DEFAULT:
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.EXTRA_PREREQS
    scripts_test.go:1788: .EXTRA_PREREQS (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.EXTRA_PREREQS` (`.EXTRA_PREREQS := nothing`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_an_expansion
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.POSIX:
    scripts_test.go:1788: .POSIX: (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.POSIX`: it changes how make invokes the shell and what -e means. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/override_.SHELLFLAGS
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/define_MAKEFLAGS
    scripts_test.go:1788: define MAKEFLAGS (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `MAKEFLAGS` (`define MAKEFLAGS`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$@
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.DEFAULT:
    scripts_test.go:1788: .DEFAULT: (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.DEFAULT`: its recipe is run for any target or prerequisite with no rule of its own. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/$(MAKE)_in_a_recipe
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_an_expansion
    scripts_test.go:1788: recipe that begins with an expansion (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:233 is a recipe line that BEGINS with an expansion (`$(Q)true`). What it expands to — possibly a `-` or `+` prefix — cannot be determined without running make. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_private_SHELL
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/override_.SHELLFLAGS
    scripts_test.go:1788: override .SHELLFLAGS (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.SHELLFLAGS` (`override .SHELLFLAGS := -c`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/MFLAGS_=_-i
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$@
    scripts_test.go:1788: recipe that begins with $@ (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:232 is a recipe line that BEGINS with an expansion (`$@-is-not-run`). What it expands to — possibly a `-` or `+` prefix — cannot be determined without running make. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/pattern-specific_SHELL
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/$(MAKE)_in_a_recipe
    scripts_test.go:1788: $(MAKE) in a recipe (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:232 names $(MAKE) in a recipe; make runs such a line even under -n and -q. | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/GNUMAKEFLAGS_+=_-i
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/a_special_target_that_is_not_the_first_word
    scripts_test.go:1782: exit 1; want "more than one target" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakea_special_target4286737125/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 da71022da1ba… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `ci`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `fmt-check`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `vet`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `echo-containment`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `build`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `contract-drift`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `test`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `tidy-check`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          FAIL  make resolves MAKEFLAGS to 'pni' while resolving `vendor-contract-selftest`.
                This guard invoked `make -pn`, so 'p' and 'n' are its own; everything else was added: i
                `-i` / `--ignore-errors` makes make ignore EVERY recipe's exit status, so `make ci` exits 0
                with the whole gate failing underneath it. `-k`, `-t` and `-q` are the same class.
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: FAILED
        A make-driven gate that can be turned into a no-op is not a gate. Fix the Makefile.
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/MAKEFLAGS_+=_-i
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_beginning_with_$@
    scripts_test.go:1782: exit 0; want "inline `;` recipe" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakeinline__recipe_3805622732/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 c3de4ec81cc8… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.SHELLFLAGS_:=_-c
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_private_SHELL
    scripts_test.go:1788: target-specific private SHELL (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `SHELL` (`test: private SHELL = /bin/sh`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.SECONDEXPANSION:
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_+_prefix
    scripts_test.go:1782: exit 0; want "inline `;` recipe" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakeinline__recipe_1610996473/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 a447d954582a… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/grouped_targets_&:
    scripts_test.go:1782: exit 0; want "more than one target" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakegrouped_targets_338978344/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 6179220acc49… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_(all_targets)
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/.ONESHELL:
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/MFLAGS_=_-i
    scripts_test.go:1788: MFLAGS = -i (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `MFLAGS` (`MFLAGS = -i`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/+_recipe_line
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/pattern-specific_SHELL
    scripts_test.go:1788: pattern-specific SHELL (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `SHELL` (`%: SHELL := /usr/bin/true`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/define_.RECIPEPREFIX
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/GNUMAKEFLAGS_+=_-i
    scripts_test.go:1788: GNUMAKEFLAGS += -i (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `GNUMAKEFLAGS` (`GNUMAKEFLAGS += -i`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/override_.RECIPEPREFIX_=_>
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/MAKEFLAGS_+=_-i
    scripts_test.go:1788: MAKEFLAGS += -i (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `MAKEFLAGS` (`MAKEFLAGS += -i`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_-_prefix
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.SHELLFLAGS_:=_-c
    scripts_test.go:1788: .SHELLFLAGS := -c (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.SHELLFLAGS` (`.SHELLFLAGS := -c`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== CONT  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_MAKEFLAGS
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_(all_targets)
    scripts_test.go:1788: .IGNORE: (all targets) (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.IGNORE`: every recipe failure of the named targets (or of ALL targets) is ignored — a `-` prefix no reading can see. | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/define_.RECIPEPREFIX
    scripts_test.go:1788: define .RECIPEPREFIX (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.RECIPEPREFIX` (`define .RECIPEPREFIX`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.SECONDEXPANSION:
    scripts_test.go:1788: .SECONDEXPANSION: (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.SECONDEXPANSION`: prerequisites would be expanded again while make considers targets, which happens under -n and -q too. | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/+_recipe_line
    scripts_test.go:1788: + recipe line (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:232 is a recipe line prefixed `+`, which make runs even under -n and -q: `+@true` | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/.ONESHELL:
    scripts_test.go:1788: .ONESHELL: (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 declares `.ONESHELL`: the whole recipe goes to one shell, so a prefix applies to its first line only. | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/override_.RECIPEPREFIX_=_>
    scripts_test.go:1788: override .RECIPEPREFIX = > (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `.RECIPEPREFIX` (`override .RECIPEPREFIX = >`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_MAKEFLAGS
    scripts_test.go:1788: target-specific MAKEFLAGS (re-pinned) | anchor exit 1, 0 make processes: FAIL  Makefile:231 assigns `MAKEFLAGS` (`test: MAKEFLAGS += -i`). SHELL and .SHELLFLAGS may appear only as ['.SHELLFLAGS := -eu -o pipefail -c', 'SHELL := /bin/bash']; MAKEFLAGS, GNUMAKEFLAGS, MFLAGS, .RECIPEPREFIX and .EXTRA_PREREQS may not be assigned in any literal form this reading matches (global, target- or pattern-specific, define, private, override). | ci-required-guard exit 1
=== NAME  TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_-_prefix
    scripts_test.go:1782: exit 0; want "inline `;` recipe" refused with 0 make processes started:
        make-integrity-guard: /private/var/folders/f0/7491x4mn6r90h50q54n7bs440000gn/T/TestNamedMakefileConstructsAreRefusedBeforeMakeinline__recipe_3752157815/001
          gate targets: ci, fmt-check, vet, echo-containment, build, contract-drift, test, tidy-check, vendor-contract-selftest
          ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
          ok    `make` resolves to /usr/bin/make (a regular file named make in an approved system directory), and every make this guard starts is started by that path. This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
          ok    [--workflow] none of the 7 variable(s) the makefiles take from the environment is set: ['BUILD_TIME', 'COMMIT', 'CORE', 'CORE_REMOTE', 'GOFLAGS', 'IMAGE', 'VERSION']
          ok    make runs only on REVIEWED bytes: Makefile sha256 1f56f0b814b3… match .github/pinned-makefiles.yml; `make -q` (one invocation, every pinned file a goal) says none would be remade; make is /usr/bin/make. Those bytes run their own reviewed parse-time call(s) while read: Makefile:31 `GOFMT       := $(shell go env GOROOT)/bin/gofmt`; Makefile:33 `VERSION     ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo `; Makefile:34 `COMMIT      ?= $(shell git rev-parse HEAD 2>/dev/null || echo unknown)`; Makefile:35 `BUILD_TIME  ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. What a reviewer approves together with a pin update is outside this check.
          ok    MAKEFILE_LIST is exactly the pinned set: ['Makefile']
          closure: 10 target(s); 1 reached through prerequisites (test-noskip)
          ok    make read 1 makefile(s): Makefile
          ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
          ok    gate target `ci` is defined exactly once (Makefile:51)
          ok    gate target `fmt-check` is defined exactly once (Makefile:54)
          ok    gate target `vet` is defined exactly once (Makefile:62)
          ok    gate target `echo-containment` is defined exactly once (Makefile:66)
          ok    gate target `build` is defined exactly once (Makefile:75)
          ok    gate target `contract-drift` is defined exactly once (Makefile:131)
          ok    gate target `test` is defined exactly once (Makefile:137)
          ok    gate target `tidy-check` is defined exactly once (Makefile:155)
          ok    gate target `vendor-contract-selftest` is defined exactly once (Makefile:196)
          ok    gate target `test-noskip` is defined exactly once (Makefile:148)
          ok    `ci`: make resolves SHELL to the approved /bin/bash
          ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `ci`: `.ONESHELL:` is not in effect
          ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `ci`: make reports no duplicate-definition override
          ok    `ci`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `fmt-check`: make resolves SHELL to the approved /bin/bash
          ok    `fmt-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `fmt-check`: `.ONESHELL:` is not in effect
          ok    `fmt-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `fmt-check`: make reports no duplicate-definition override
          ok    `fmt-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vet`: make resolves SHELL to the approved /bin/bash
          ok    `vet`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vet`: `.ONESHELL:` is not in effect
          ok    `vet`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vet`: make reports no duplicate-definition override
          ok    `vet`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `echo-containment`: make resolves SHELL to the approved /bin/bash
          ok    `echo-containment`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `echo-containment`: `.ONESHELL:` is not in effect
          ok    `echo-containment`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `echo-containment`: make reports no duplicate-definition override
          ok    `echo-containment`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `build`: make resolves SHELL to the approved /bin/bash
          ok    `build`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `build`: `.ONESHELL:` is not in effect
          ok    `build`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `build`: make reports no duplicate-definition override
          ok    `build`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `contract-drift`: make resolves SHELL to the approved /bin/bash
          ok    `contract-drift`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `contract-drift`: `.ONESHELL:` is not in effect
          ok    `contract-drift`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `contract-drift`: make reports no duplicate-definition override
          ok    `contract-drift`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `test`: make resolves SHELL to the approved /bin/bash
          ok    `test`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `test`: `.ONESHELL:` is not in effect
          ok    `test`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `test`: make reports no duplicate-definition override
          ok    `test`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `tidy-check`: make resolves SHELL to the approved /bin/bash
          ok    `tidy-check`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `tidy-check`: `.ONESHELL:` is not in effect
          ok    `tidy-check`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `tidy-check`: make reports no duplicate-definition override
          ok    `tidy-check`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `vendor-contract-selftest`: make resolves SHELL to the approved /bin/bash
          ok    `vendor-contract-selftest`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
          ok    `vendor-contract-selftest`: `.ONESHELL:` is not in effect
          ok    `vendor-contract-selftest`: MAKEFLAGS carries nothing beyond this guard's own -pn ('pn')
          ok    `vendor-contract-selftest`: make reports no duplicate-definition override
          ok    `vendor-contract-selftest`: no expanded command discards its exit status (the one contract-drift `ran` line excepted)
          ok    `make -q Makefile` again at the end: still nothing would be remade
        make-integrity-guard: passed (9 gate target(s); make ran 21 time(s), by /usr/bin/make, only on the pinned bytes of Makefile, which are unchanged)
--- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake (0.05s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.RECIPEPREFIX_:=_>_with_a_>_-true_recipe (0.36s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$< (0.37s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/$(eval_…) (0.39s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/$(call_eval,…) (1.80s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_target-specific (1.81s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/two_targets_on_one_rule_line,_a_gate_target_second (1.83s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/define_with_a_computed_name (1.88s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_computed_variable_name,_global (1.88s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_continued_with_a_backslash (1.81s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_line_that_starts_with_whitespace (1.80s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_rule_target_that_is_an_expansion_($(I)ORE:) (1.96s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$X_(a_one-letter_variable) (0.65s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/SHELL_:=_/usr/bin/true (0.51s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_.EXTRA_PREREQS (0.37s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_test (0.56s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.EXTRA_PREREQS (0.34s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.POSIX: (0.27s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/define_MAKEFLAGS (0.32s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.DEFAULT: (0.36s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_an_expansion (0.40s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/override_.SHELLFLAGS (0.35s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/recipe_that_begins_with_$@ (0.35s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/$(MAKE)_in_a_recipe (0.39s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/a_special_target_that_is_not_the_first_word (1.72s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_beginning_with_$@ (1.76s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_private_SHELL (0.40s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_+_prefix (1.91s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/grouped_targets_&: (1.87s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/MFLAGS_=_-i (0.43s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/pattern-specific_SHELL (0.37s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/GNUMAKEFLAGS_+=_-i (0.29s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/MAKEFLAGS_+=_-i (0.30s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.SHELLFLAGS_:=_-c (0.34s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.IGNORE:_(all_targets) (0.36s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/define_.RECIPEPREFIX (0.33s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.SECONDEXPANSION: (0.41s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/+_recipe_line (0.37s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/.ONESHELL: (0.39s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/override_.RECIPEPREFIX_=_> (0.32s)
    --- PASS: TestNamedMakefileConstructsAreRefusedBeforeMake/target-specific_MAKEFLAGS (0.23s)
    --- FAIL: TestNamedMakefileConstructsAreRefusedBeforeMake/inline_;_recipe_with_a_-_prefix (1.14s)
=== RUN   TestEnvironmentTakenVariablesNeverReachMake
=== RUN   TestEnvironmentTakenVariablesNeverReachMake/contract-drift-guard.py_recipe
=== PAUSE TestEnvironmentTakenVariablesNeverReachMake/contract-drift-guard.py_recipe
=== RUN   TestEnvironmentTakenVariablesNeverReachMake/makegate.py_--_--dry-run_contract-drift
=== PAUSE TestEnvironmentTakenVariablesNeverReachMake/makegate.py_--_--dry-run_contract-drift
=== CONT  TestEnvironmentTakenVariablesNeverReachMake/contract-drift-guard.py_recipe
=== CONT  TestEnvironmentTakenVariablesNeverReachMake/makegate.py_--_--dry-run_contract-drift
=== NAME  TestEnvironmentTakenVariablesNeverReachMake/contract-drift-guard.py_recipe
    scripts_test.go:1892: contract-drift-guard.py recipe: a make process received a variable the Makefile can read from the environment: MAKE-ENV -q Makefile | planted: VZ_M4_SUBST,Q,VZ_M4_ORIGIN,VZ_M4_VALUE,VZ_M4_EARLY,VZ_M4_IFDEF
    scripts_test.go:1892: contract-drift-guard.py recipe: a make process received a variable the Makefile can read from the environment: MAKE-ENV --dry-run --no-print-directory | planted: VZ_M4_SUBST,Q,VZ_M4_ORIGIN,VZ_M4_VALUE,VZ_M4_EARLY,VZ_M4_IFDEF
=== NAME  TestEnvironmentTakenVariablesNeverReachMake/makegate.py_--_--dry-run_contract-drift
    scripts_test.go:1892: makegate.py -- --dry-run contract-drift: a make process received a variable the Makefile can read from the environment: MAKE-ENV -q Makefile | planted: VZ_M4_SUBST,Q,VZ_M4_ORIGIN,VZ_M4_VALUE,VZ_M4_EARLY,VZ_M4_IFDEF
    scripts_test.go:1892: makegate.py -- --dry-run contract-drift: a make process received a variable the Makefile can read from the environment: MAKE-ENV --dry-run --no-print-directory | planted: VZ_M4_SUBST,Q,VZ_M4_ORIGIN,VZ_M4_VALUE,VZ_M4_EARLY,VZ_M4_IFDEF
--- FAIL: TestEnvironmentTakenVariablesNeverReachMake (0.05s)
    --- FAIL: TestEnvironmentTakenVariablesNeverReachMake/contract-drift-guard.py_recipe (0.17s)
    --- FAIL: TestEnvironmentTakenVariablesNeverReachMake/makegate.py_--_--dry-run_contract-drift (0.17s)
FAIL
FAIL	github.com/yegamble/vizra-search/scripts	10.099s
FAIL
exit=1
